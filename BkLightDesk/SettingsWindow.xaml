<Window x:Class="BkLightDesk.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Impostazioni" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E">

    <Window.Resources>
        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Grid Width="44" Height="24">
                                <Border x:Name="Track" Background="#3E3E42" CornerRadius="12" BorderThickness="1" BorderBrush="#555"/>
                                <Ellipse x:Name="Thumb" Width="18" Height="18" Fill="White" HorizontalAlignment="Left" Margin="3,0,0,0">
                                    <Ellipse.Effect>
                                        <DropShadowEffect ShadowDepth="1" BlurRadius="2" Opacity="0.3"/>
                                    </Ellipse.Effect>
                                </Ellipse>
                            </Grid>
                            <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="#007ACC"/>
                                <Setter TargetName="Track" Property="BorderBrush" Value="#007ACC"/>
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,3,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="IMPOSTAZIONI" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

            <GroupBox Header="Trasmissione Bluetooth" Foreground="#888" BorderBrush="#444" Margin="0,0,0,15" Padding="10">
                <StackPanel>
                    <CheckBox x:Name="ChkTurbo" 
                              Content="🚀 Modalità Turbo" 
                              Style="{StaticResource ToggleSwitchStyle}"
                              Checked="ChkTurbo_Changed"
                              Unchecked="ChkTurbo_Changed"/>
                    
                    <TextBlock Text="Attiva per invii veloci. Disattiva se noti errori grafici." 
                               Foreground="#666" FontSize="11" Margin="54,5,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </GroupBox>

            <Button Content="Salva e Chiudi" Height="35" Background="#2D2D30" Foreground="White" BorderThickness="0" Click="BtnClose_Click" Margin="0,20,0,0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>