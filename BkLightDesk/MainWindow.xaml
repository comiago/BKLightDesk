<Window x:Class="BkLightDesk.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BK Light Controller (32x32)" Height="550" Width="600"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        Background="#121212">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3E3E42"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#1E1E1E"/>
                                <Setter Property="Foreground" Value="#555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="LED MATRIX COMMANDER" Foreground="#888888" FontSize="12" HorizontalAlignment="Center"/>
            <TextBlock Text="BkLightDesk 32x32" Foreground="White" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center"/>
            <Separator Margin="50,15,50,0" Background="#333333"/>
        </StackPanel>

        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            
            <Button x:Name="BtnScan" Content="📡  RICERCA DISPOSITIVO" Height="50" Click="BtnScan_Click" FontSize="16" FontWeight="SemiBold"/>
            
            <Separator Background="#222222" Margin="10,25"/>

            <Button x:Name="BtnRedTest" Content="🔴 TEST ROSSO" Height="40" Click="BtnRedTest_Click" IsEnabled="False"/>
            <Button x:Name="BtnLoadImage" Content="📂 CARICA IMMAGINE" Height="40" Margin="0,10,0,0" Click="BtnLoadImage_Click" IsEnabled="False" Background="#007ACC"/>
            
            <Button x:Name="BtnClock" Content="🕒 OROLOGIO STYLE" Height="40" Margin="0,10,0,0" Click="BtnClock_Click" IsEnabled="False" Background="#6600CC"/>

            <Grid Margin="5,25,5,0">
                
                <Button x:Name="BtnSettings" 
                        Content="⚙️ Impostazioni" 
                        Style="{StaticResource LinkButtonStyle}"
                        HorizontalAlignment="Left" 
                        VerticalAlignment="Center"
                        Click="BtnSettings_Click"/>

                <Button x:Name="BtnLogs" 
                        Content="Vedi Logs 📝" 
                        Style="{StaticResource LinkButtonStyle}"
                        HorizontalAlignment="Right" 
                        VerticalAlignment="Center"
                        Click="BtnLogs_Click"/>
            </Grid>
        </StackPanel>

        <Border Grid.Row="2" Background="#1E1E1E" CornerRadius="8" Padding="15" Margin="0,20,0,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Ellipse x:Name="StatusLed" Width="12" Height="12" Fill="#FF4444" Margin="0,0,12,0"/>
                <TextBlock x:Name="TxtStatus" Text="Disconnesso" Foreground="#FF4444" FontSize="15" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>