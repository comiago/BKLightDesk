<Window x:Class="BkLightDesk.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BK Light Controller (32x32)" Height="550" Width="600"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        Background="#121212">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3E3E42"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#1E1E1E"/>
                                <Setter Property="Foreground" Value="#555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Grid Width="44" Height="24">
                                <Border x:Name="Track" 
                                        Background="#3E3E42" 
                                        CornerRadius="12" 
                                        BorderThickness="1"
                                        BorderBrush="#555"/>
                                
                                <Ellipse x:Name="Thumb" 
                                         Width="18" Height="18" 
                                         Fill="White"
                                         HorizontalAlignment="Left" 
                                         Margin="3,0,0,0">
                                    <Ellipse.Effect>
                                        <DropShadowEffect ShadowDepth="1" BlurRadius="2" Opacity="0.3"/>
                                    </Ellipse.Effect>
                                </Ellipse>
                            </Grid>
                            
                            <ContentPresenter Margin="10,0,0,0" 
                                              VerticalAlignment="Center" 
                                              RecognizesAccessKey="True"/>
                        </StackPanel>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="#007ACC"/>
                                <Setter TargetName="Track" Property="BorderBrush" Value="#007ACC"/>
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,3,0"/>
                            </Trigger>
                            
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Track" Property="Background" Value="#222"/>
                                <Setter TargetName="Track" Property="BorderBrush" Value="#333"/>
                                <Setter TargetName="Thumb" Property="Fill" Value="#555"/>
                                <Setter Property="Foreground" Value="#555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="LED MATRIX COMMANDER" Foreground="#888888" FontSize="12" HorizontalAlignment="Center"/>
            <TextBlock Text="BkLightDesk 32x32" Foreground="White" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center"/>
            <Separator Margin="50,15,50,0" Background="#333333"/>
        </StackPanel>

        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            
            <Button x:Name="BtnScan" Content="📡  RICERCA DISPOSITIVO" Height="50" Click="BtnScan_Click" FontSize="16" FontWeight="SemiBold"/>
            
            <Separator Background="#222222" Margin="10,25"/>

            <Button x:Name="BtnRedTest" Content="🔴 TEST ROSSO" Height="40" Click="BtnRedTest_Click" IsEnabled="False"/>
            <Button x:Name="BtnLoadImage" Content="📂 CARICA IMMAGINE" Height="40" Margin="0,10,0,0" Click="BtnLoadImage_Click" IsEnabled="False" Background="#007ACC"/>

            <Grid Margin="5,25,5,0">
                <CheckBox x:Name="ChkTurbo" 
                          Content="🚀 Turbo Mode" 
                          IsChecked="True"
                          Style="{StaticResource ToggleSwitchStyle}"
                          HorizontalAlignment="Left"
                          VerticalAlignment="Center"/>

                <Button x:Name="BtnLogs" Content="Vedi Logs" Background="Transparent" 
                        Foreground="#007ACC" FontSize="12" 
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Cursor="Hand" Click="BtnLogs_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <TextBlock Text="{TemplateBinding Content}" TextDecorations="Underline"/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </StackPanel>

        <Border Grid.Row="2" Background="#1E1E1E" CornerRadius="8" Padding="15" Margin="0,20,0,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Ellipse x:Name="StatusLed" Width="12" Height="12" Fill="#FF4444" Margin="0,0,12,0"/>
                <TextBlock x:Name="TxtStatus" Text="Disconnesso" Foreground="#FF4444" FontSize="15" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>